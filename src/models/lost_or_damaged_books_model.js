class lost_or_damaged_books_model {
  constructor(book_id, status, reported_date, description, created_at, updated_at) {
    (this.book_id = book_id), (this.status = status), (this.reported_date = reported_date), (this.description = description), (this.created_at = created_at), (this.updated_at = updated_at);
  }
}

export default lost_or_damaged_books_model;
