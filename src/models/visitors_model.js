class visitors_model {
  constructor(name, email, phone_number, visit_date, created_at, updated_at) {
    (this.name = name), (this.email = email), (this.phone_number = phone_number), (this.visit_date = visit_date), (this.created_at = created_at), (this.updated_at = updated_at);
  }
}

export default visitors_model;
